---
last_mapped: REPLACE_TIMESTAMP
total_files: REPLACE_COUNT
total_files_scanned: REPLACE_COUNT
total_functions: REPLACE_COUNT
total_classes: REPLACE_COUNT
static_analysis: REPLACE_METHOD
analysis_method: REPLACE_CODEWIKI_OR_TREESITTER
confidence: REPLACE_LEVEL
generated_docs:
  - CODEBASE_MAP.md
---

# Codebase Map

> Auto-generated by Codebase Oracle. Last mapped: REPLACE_TIMESTAMP
> Analysis method: REPLACE (CodeWiki or Tree-sitter)

<!-- ORACLE:OVERVIEW
Lead fills this from c4-architecture.md System Context section.
Summarize in 2-3 sentences: what the system is, what it does, who it's for.
-->

## System Overview

REPLACE: 2-3 sentence summary

**Stack**: REPLACE: key technologies (language, framework, database, etc.)
**Architecture**: REPLACE: pattern name (monolith, microservices, serverless, etc.)
**Analysis Method**: REPLACE: "CodeWiki (LLM-powered)" or "Tree-sitter (AST-based)"

## Static Analysis Metrics

<!-- ORACLE:METRICS
Include this section based on analysis method used.

If CodeWiki was used (check .codewiki-cache/):
- Load module_tree.json for structure
- Load dependency_graph.json for nodes/edges count
- Load call_graph.json for call relationships
- Component count from components/ directory

If Tree-sitter was used:
- Metrics from .tree-sitter-results.json:
- summary.total_functions: total functions discovered
- summary.total_classes: total classes/types discovered
- summary.total_imports: total imports found
- summary.total_exports: total exports found
- summary.by_language: breakdown of files by language
- available_languages: which languages were parsed
-->

| Metric | Count |
|--------|-------|
| Files Analyzed | REPLACE |
| Functions/Methods | REPLACE |
| Classes/Types | REPLACE |
| Dependencies | REPLACE |
| Call Relationships | REPLACE |

**Analysis Source**: REPLACE (CodeWiki or Tree-sitter)
**Languages**: REPLACE

## Architecture Docs

<!-- ORACLE:DOC_INDEX
Only include rows for docs that were actually generated.
Remove rows for docs that were skipped.
-->

| Document | Description |
|----------|-------------|
| [C4 Architecture](c4-architecture.md) | System context, containers, components |
| [Key Flows](key-flows.md) | Sequence diagrams for critical paths |
| [Dependency Graph](dependency-graph.md) | Module dependencies, hub analysis |

<!-- ORACLE:CONDITIONAL_DOCS
Add these rows ONLY if the corresponding doc was generated:
| [Data Model](data-model.md) | Entity relationships, schemas |
| [API Surface](api-surface.md) | Routes, endpoints, auth |
| [Product Requirements](product-requirements.md) | Features, user roles, business rules |
| [Infrastructure](infrastructure.md) | Deployment, CI/CD, config |
-->

## Hub Files

<!-- ORACLE:HUBS
Lead fills this from dependency-graph.md Hub Analysis table.
Copy the top hubs (5+ dependents) with their dependent count and role.

Primary sources (in order of preference):
1. CodeWiki dependency_graph.json: Count incoming edges per node
2. Tree-sitter `hubs` array from .tree-sitter-results.json
3. Grep-based dependency counting

For CodeWiki:
```bash
cat .codewiki-cache/dependency_graph.json | python3 -c "
import json, sys
from collections import Counter
d = json.load(sys.stdin)
dependents = Counter()
for edge in d.get('edges', []):
    dependents[edge.get('to')] += 1
for file, count in dependents.most_common(10):
    print(f'{file}: {count} dependents')
"
```

For Tree-sitter hub format: [{"file": "src/utils.ts", "dependents": 12}, ...]
-->

| Package | Dependents | Stability | Risk |
|---------|-----------|-----------|------|
| REPLACE | REPLACE | REPLACE | REPLACE |

## Directory Structure

<!-- ORACLE:TREE
Lead fills this from scanner output or CodeWiki module tree.
If CodeWiki is available, use module_tree.json for accurate structure:
```bash
cat .codewiki-cache/module_tree.json | python3 -c "
import json, sys
def print_tree(tree, indent=0):
    for name, data in tree.items():
        print('  ' * indent + name + '/')
        if 'children' in data:
            print_tree(data['children'], indent + 1)
print_tree(json.load(sys.stdin))
"
```

Add one-line purpose comment after each directory.
Only show directories and key files, not every file.
-->

```
REPLACE: annotated directory tree
```

## Module Guide

<!-- ORACLE:MODULES
Lead fills this from c4-architecture.md Component section and dependency-graph.md.
If CodeWiki is available, use module_tree.json for module structure:
- Each module has: name, path, components, children
- Components list shows core files/classes per module

Create one subsection per top-level module/directory.
For each module: purpose, layer, key files.
-->

### REPLACE: Module Name

**Purpose**: REPLACE
**Layer**: REPLACE
**Entry point**: REPLACE

| Directory | Purpose | Layer |
|-----------|---------|-------|
| REPLACE | REPLACE | REPLACE |

## Conventions

<!-- ORACLE:CONVENTIONS
Lead fills this from patterns observed across analyst docs.
Look for naming patterns, file organization, coding patterns in the filled docs.
-->

REPLACE: naming conventions, patterns, style guide observations

## Gotchas

<!-- ORACLE:GOTCHAS
Lead fills this from analyst findings — unusual behaviors, edge cases,
warnings flagged with △ in other docs.
-->

REPLACE: non-obvious behaviors, warnings

## Navigation Guide

<!-- ORACLE:NAVIGATION
Lead fills this from module structure and api-surface.md patterns.
Common developer tasks and which files to touch.
-->

REPLACE: task-to-files mapping

## Confidence Assessment

Overall: REPLACE_BAR REPLACE_PERCENTAGE%

**High confidence:**
- REPLACE — REPLACE_REASON

**Lower confidence:**
- REPLACE — REPLACE_REASON

## △ Caveats

<!-- ORACLE:CAVEATS
Include this section if overall confidence < 5.
List assumptions made and gaps in analysis.
-->

REPLACE: assumptions and gaps (delete section if confidence = 5)
