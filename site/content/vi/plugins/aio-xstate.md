---
title: "aio-xstate"
description: "Build state machine XState v5 đúng cách — lập kế hoạch design-first, typing params-first và pattern actor chuẩn cho TypeScript và React, kèm enforcement anti-god-machine."
document_type: "plugin"
version: "1.0.8"
install: "/plugin install aio-xstate@aiocean-plugins"
skills_count: 1
---

> **Cài đặt:** `/plugin install aio-xstate@aiocean-plugins` · `v1.0.8`

# aio-xstate

Bộ rule strict XState v5 cho state machine TypeScript. Pattern setup().createMachine(), type actor, lập kế hoạch design-first, enforcement anti-god-machine và pattern chuẩn.

## Cài đặt

```bash
/plugin install aio-xstate@aiocean-plugins
```

## Làm gì

- Lập kế hoạch design-first: model state và transition trước khi viết code
- Implement types-first với `setup().createMachine()`
- Type actor: promise, callback, observable và child machine actor
- Enforcement anti-god-machine: hướng dẫn phân rã machine quá lớn
- Pattern XState v5 chuẩn và note migrate từ v4

## Yêu cầu

- TypeScript 5.0+

## Skills (1)

- [**aio-xstate**](/vi/plugins/aio-xstate/aio-xstate) — Implement state machine XState v5 với pattern strict — setup().createMachine(), actor và typing TypeScript.
